% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report_helper.R
\name{add_parent_scope}
\alias{add_parent_scope}
\title{Add parent scope (center) column to child scope (patient) data frame}
\usage{
add_parent_scope(
  transfers,
  scopeModelIdTrans = "Patient",
  parentScopeModelIdTrans = "Center",
  df,
  scopeModelIdDf = "PATIENT",
  parentScopeModelIdDf = "CENTER",
  parentCodePattern = "^...."
)
}
\arguments{
\item{transfers}{Data frame. Transfer records containing scope and parent
scope assignments.}

\item{scopeModelIdTrans}{Character. Column name for child scope identifiers
in the transfers data frame. Default: \code{"Patient"}.}

\item{parentScopeModelIdTrans}{Character. Column name for parent scope
identifiers in the transfers data frame. Default: \code{"Center"}.}

\item{df}{Data frame. Data frame to which the parent scope column will be
added.}

\item{scopeModelIdDf}{Character. Column name for child scope identifiers
in \code{df}. Default: \code{"PATIENT"}.}

\item{parentScopeModelIdDf}{Character. Column name for parent scope
identifiers to be added to \code{df}. Default: \code{"CENTER"}.}

\item{parentCodePattern}{Character. Regex pattern to extract parent code
from child code. Default: \code{"^...."} (first 4 characters).}
}
\value{
Data frame with added parent scope column as the first column,
  followed by all original columns.
}
\description{
Adds a parent scope (center) column to a data frame of child scopes
(patients). Extracts parent codes from child codes using a regex pattern,
and updates assignments based on active transfers (those with no stop date).
}
