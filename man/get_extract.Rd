% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rodano_api_communication.R
\name{get_extract}
\alias{get_extract}
\title{Extract data table from platform}
\usage{
get_extract(
  urlBase,
  auth,
  expName,
  maxAttempts = 5,
  guessMax = 0,
  includeModifDate = FALSE,
  scopePk = NULL
)
}
\arguments{
\item{urlBase}{Character. The base URL to the platform's API.}

\item{auth}{An authentication object created by \code{\link{create_authentication}}.}

\item{expName}{Character. The data table model ID to retrieve.}

\item{maxAttempts}{Integer. Maximum number of retry attempts in case of
failure (default: 5).}

\item{guessMax}{Integer. Number of rows to scan for column type inference.
The CSV parser (via \code{httr::content()} using \code{readr::read_csv()})
examines the first N rows to infer the data type of each column (numeric,
character, date, etc.). If later rows contain values incompatible with the
inferred type, they may be coerced or read as NA. Options:
\itemize{
  \item 0 (default): Uses readr's default behavior (1,000 rows)
  \item Positive integer: Scans specified number of rows
  \item -1: Scans all rows (most accurate but slowest)
}
Increase this value if you encounter unexpected type coercion, especially
in datasets where certain column types only become apparent after many rows.}

\item{includeModifDate}{Logical. Should the export include modification
dates of fields? (default: FALSE).}

\item{scopePk}{Integer or NULL. Optional scope primary key to filter the
extract to a specific scope.}
}
\value{
A data frame containing the extracted table data.
}
\description{
Retrieves a CSV data export for a specified table from the platform API
and returns it as a data frame. Includes retry logic and column type
inference configuration.
}
