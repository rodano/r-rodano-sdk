% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report_helper.R
\name{transform_unknown_dates}
\alias{transform_unknown_dates}
\title{Transform dates with unknown components to complete dates}
\usage{
transform_unknown_dates(
  dates,
  round = "middle",
  unknownPattern = "Unknown",
  defaultYear = NA
)
}
\arguments{
\item{dates}{Character vector of dates to be checked, with format dd.mm.yyyy.
Unknown components are indicated by the \code{unknownPattern} string.}

\item{round}{Character. Way to round unknown days and months. One of
\code{"floor"} (earliest possible date), \code{"ceil"} (latest possible
date), or \code{"middle"} (mid-point date). Default: \code{"middle"}.}

\item{unknownPattern}{Character. String to consider as unknown part of date.
Default: \code{"Unknown"}.}

\item{defaultYear}{Integer or Character. Year with which to replace year
when year is unknown. If \code{NA} (default), an unknown year will be
considered unacceptable and an error will be thrown if found.}
}
\value{
Character vector of dates free from unknown parts, in the same
  format as input dates (dd.mm.yyyy).
}
\description{
Manages unknown components in dates and replaces them with numbers based
on the specified rounding strategy. The unknown pattern is matched as an
exact string, not a regular expression.
}
