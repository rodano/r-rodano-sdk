% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rodano_api_communication.R
\name{get_report_resilient}
\alias{get_report_resilient}
\title{Extract workflow summary report with resilient parent-scope-based download}
\usage{
get_report_resilient(
  urlBase,
  auth,
  repName,
  childScopeModelId = "PATIENT",
  maxAttempts = 5,
  guessMax = -1,
  withHistory = FALSE,
  showProgress = TRUE,
  continueOnError = TRUE
)
}
\arguments{
\item{urlBase}{Character. The base URL to the platform's API.}

\item{auth}{An authentication object created by \code{\link{create_authentication}}.}

\item{repName}{Character. Name of the workflow report to retrieve.}

\item{childScopeModelId}{Character. The child scope model ID used to identify
parent scopes (default: "PATIENT"). For example, "PATIENT" will return data
chunked by CENTER parents where the user has read access.}

\item{maxAttempts}{Integer. Maximum number of retry attempts per scope
(default: 5).}

\item{guessMax}{Integer. Number of rows to scan for column type inference
(default: -1). See \code{\link{get_extract}} for details.}

\item{withHistory}{Logical. Should the export include historical workflow
data? (default: FALSE).}

\item{showProgress}{Logical. Should a progress bar be displayed? (default: TRUE).}

\item{continueOnError}{Logical. Should the download continue if a scope fails?
If TRUE, failed scopes are skipped with a warning. If FALSE, the function
stops on the first error (default: TRUE).}
}
\value{
A data frame containing the workflow report data from all parent scopes.
}
\description{
Retrieves a workflow summary report by breaking the download into chunks based on
parent scopes (e.g., centers). This approach is more resilient for large
datasets that may timeout when downloaded as a single request. The function
queries for available parent scopes where the user has read access, then
downloads the report for each parent scope separately and combines the results.
}
\seealso{
\code{\link{get_report}} for retrieving a single workflow report.
}
