% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report_helper.R
\name{partial_date_to_date}
\alias{partial_date_to_date}
\title{Convert partial dates to R Date objects or formatted strings}
\usage{
partial_date_to_date(
  str,
  format = "\%d.\%m.\%Y",
  unknownDate = FALSE,
  defaultYear = NA,
  toStr = FALSE,
  round = "middle"
)
}
\arguments{
\item{str}{Character vector of dates as strings to be transformed.}

\item{format}{Character. Format of input dates. Default: \code{"\%d.\%m.\%Y"}.
Also supports \code{"\%m.\%Y"} and \code{"\%Y"}.}

\item{unknownDate}{Logical. Indicates whether dates may contain unknown
parts. Default: \code{FALSE}.}

\item{defaultYear}{Integer or Character. Year to use when year is unknown.
Default: \code{NA}.}

\item{toStr}{Logical. If \code{TRUE}, returns dates as strings with format
\%Y-\%m-\%d. If \code{FALSE} (default), returns R Date objects.}

\item{round}{Character. Way to round unknown days and months. One of
\code{"floor"}, \code{"ceil"}, or \code{"middle"}. Default:
\code{"middle"}.}
}
\value{
Date vector of R Date objects (when \code{toStr = FALSE}) or
  character vector of formatted date strings (when \code{toStr = TRUE}).
  Returns input unchanged if length is zero.
}
\description{
Converts partial dates from string format to R Date objects (default) or
to formatted strings with format %Y-%m-%d. Handles dates with unknown
components by transforming them according to the specified rounding strategy.
}
