% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report_helper.R
\name{print_table_to_csv}
\alias{print_table_to_csv}
\title{Write check results to CSV with change tracking}
\usage{
print_table_to_csv(
  projectName,
  tableResult,
  checkId,
  previousReportFolder,
  outDate = today,
  outputPath
)
}
\arguments{
\item{projectName}{Character. Name of the project.}

\item{tableResult}{Data frame of check results to export.}

\item{checkId}{Character. Identifier for the check (used in filename and
as a column value).}

\item{previousReportFolder}{List with elements \code{folder_path} and
\code{date}, as returned by \code{\link{get_previous_report_folder}}.}

\item{outDate}{Date used in the output filename.}

\item{outputPath}{Character. Directory where the CSV will be written.}
}
\value{
The enriched \code{tableResult} data frame (invisibly \code{NULL}
  when \code{tableResult} has zero rows).
}
\description{
Writes a dataframe of check results to a CSV file, assigning each row a
unique MD5-based identifier. If a previous report exists for the same check,
the function carries forward \code{resolved} and \code{comment} values from
matching rows, enabling incremental review of findings across report runs.
}
