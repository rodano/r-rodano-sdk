% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report_helper.R
\name{report_findings}
\alias{report_findings}
\title{Run all checks and produce a findings report with summary}
\usage{
report_findings(projectName, reportName, results, previousReportDate, outDir, outDate = Sys.Date())
}
\arguments{
\item{projectName}{Character. Name of the project.}

\item{reportName}{Character. Report identifier (e.g.
\code{"data_validation"}).}

\item{results}{A list of check results. Each element must be a list with
\code{table_result} (data frame), \code{check_id} (character), and
\code{description} (character).}

\item{previousReportDate}{Date, or \code{NULL}. Date of the
previous report to use for comparison. If \code{NULL} (default), the
function will automatically search for the most recent previous report.}

\item{outDir}{Character. Parent directory where the dated report folder
will be created.}

\item{outDate}{Date for the report (default: Sys.Date()). Used in folder and
file names, and to locate the previous report for comparison.}
}
\value{
Called for its side effects: writes per-check CSV files and a
  summary CSV into the report folder.
}
\description{
Iterates over a list of check results, writes each one to a CSV via
\code{\link{print_table_to_csv}}, and produces a summary CSV comparing
current observation counts with the previous report run. The output folder
is created automatically under \code{outDir} and named
\code{<outDate>_<reportName>}.
}
\seealso{
\code{\link{print_table_to_csv}},
  \code{\link{get_previous_report_folder}},
  \code{\link{build_output_filename}}
}
